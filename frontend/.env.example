# Base URL of the backend API. In dev, leave as /api to use the Vite proxy.
VITE_API_BASE_URL=/api

# Google OAuth client ID (same one configured on the backend), used for Sign in with Google.
VITE_GOOGLE_CLIENT_ID=
